<p id="notice"><%= notice %></p>

<h1>Workspaces</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Icon</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @workspaces.each do |workspace| %>
      <tr>
        <td><%= workspace.name %></td>
        <td><%= workspace.icon %></td>
        <td><%= link_to 'Show', workspace %></td>
        <td><%= link_to 'Edit', edit_workspace_path(workspace) %></td>
        <td><%= link_to 'Destroy', workspace, data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Workspace', new_workspace_path %>