<%=
  vue_component 'Dashboard',
    {
      user: {
        name: @user.email&.split('@')&.first&.capitalize || "User",
        email: @user.email,
        avatar: @user.respond_to?(:avatar) ? @user.avatar : nil
      },
      dashboard_stats: @dashboard_stats
    }
%>
