<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <meta name="apple-mobile-web-app-capable" content="yes" />
    <meta name="mobile-web-app-capable" content="yes" />
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <link rel="icon" href="/icon.png" type="image/png" />
    <link rel="icon" href="/icon.svg" type="image/svg+xml" />
    <link rel="apple-touch-icon" href="/icon.png" />
    <%= vite_client_tag %>
    <%= vite_typescript_tag 'application' %>
    <%= vite_typescript_tag 'components' %>
  </head>
  <body data-turbo="true" class="h-screen overflow-hidden">
    
    <!-- Background global -->
    <%= vue_component 'GradientBackground' %>
    
    <% if controller_name == 'pages' && action_name == 'landing_page' %>
      <!-- Layout pour landing page (SANS shared components) -->
      <div class="min-h-screen">
        <%= yield %>
      </div>
    <% else %>
      <!-- Layout normal AVEC shared components -->
      <div class="h-screen flex flex-col">
        <!-- Header fixe en haut -->
        <div class="h-16 w-full z-50">
          <%= render 'shared/header' %>
        </div>
        <!-- Zone principale avec sidebar + contenu -->
        <div class="flex-1 flex overflow-hidden">
          <!-- Sidebar fixe à gauche -->
          <div class="w-64 h-full z-40">
            <%= render 'shared/sidebar' %>
          </div>
          <!-- Zone de contenu principal -->
          <main class="flex-1 overflow-auto bg-transparent">
            <%= yield %>
          </main>
        </div>
      </div>
    <% end %>
    
  </body>
</html>
